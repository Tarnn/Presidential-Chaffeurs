export interface Vehicle {
  id: number;
  name: string;
  description: string;
  rate: number;
  photos: string[];
}

export interface Testimonial {
  id: number;
  text: string;
  author: string;
}
